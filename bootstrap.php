<?php

// this only exists for tests and static analysis. at the time it was
// added phpstan could not discover constants created with `define()`.

require __DIR__ . "/vendor/autoload.php";

const TESTS_DIR = __DIR__ . "/tests";
